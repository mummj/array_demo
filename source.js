'use strict'

const pet =['dog','cat','fish'];

pet.push('lizzard');

pet.push('hamster');

pet.pop('hamster');

pet.shift('dog');

console.log(pet.length);
console.log(pet[i]);
console.log(pet[0]);

